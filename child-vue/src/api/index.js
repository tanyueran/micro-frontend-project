/**
 * @author tanxin
 * @date 2019/10/15
 * @Description: 存放全局的ajax请求地址
 */
const api = '/api';

const urlObj = {
  // 登录模块
  login: {
    login: `${api}/usercenter/login`,
  },
};

export default urlObj;
